import React from "react"

import Layout from "../components/layout"
import "../less/index.less"

export default ({ data }) => (
  <Layout>
    <section>
      <h2>contact</h2>
    </section>
  </Layout>
)